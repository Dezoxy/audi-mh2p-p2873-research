#!/bin/ksh
# Wrapper installed at /mnt/app/eso/bin/apps/gal; the factory binary is at gal.real.
preload=/mnt/app/eso/bin/apps/cluster/gal_cluster.so
[[ -f "$preload" ]] && export LD_PRELOAD=$preload
exec /mnt/app/eso/bin/apps/gal.real "$@"
